<UserControl x:Class="DailyBugle.Wpf.Views.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:DailyBugle.Wpf.ViewModels"
             xmlns:conv="clr-namespace:DailyBugle.Wpf.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:UserViewModel}"
             d:DesignWidth="760">
    <UserControl.Resources>
        <conv:DeliveryStatusConverter x:Key="DeliveryStatusConverter" />
    </UserControl.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding DisplayName, StringFormat='My Alert Rules — {0}'}" FontWeight="Bold" FontSize="14" Margin="0,0,0,6" />

        <DataGrid Grid.Row="2" ItemsSource="{Binding Rules}" AutoGenerateColumns="False" IsReadOnly="True" MinHeight="90" MaxHeight="160" Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="News Type" Binding="{Binding NewsType}" Width="*" />
                <DataGridTextColumn Header="Channel" Binding="{Binding Channel}" Width="*" />
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="Auto" />
                <DataGridTemplateColumn Header="" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Remove" Padding="8,2" MinWidth="70" HorizontalAlignment="Stretch"
                                    Command="{Binding DataContext.RemoveRuleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <TextBlock Text="Add New Rule" FontWeight="Bold" FontSize="14" Margin="0,0,0,6" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="News Type:" VerticalAlignment="Center" Margin="0,0,6,0" />
                <ComboBox Grid.Column="1" ItemsSource="{Binding NewsTypes}" SelectedItem="{Binding SelectedNewsType}" />
                <TextBlock Grid.Column="2" Text="Channel:" VerticalAlignment="Center" Margin="12,0,6,0" />
                <ComboBox Grid.Column="3" ItemsSource="{Binding Channels}" SelectedItem="{Binding SelectedChannel}" />
                <Button Grid.Column="4" Content="Add Rule" HorizontalAlignment="Right" Padding="12,4"
                        Command="{Binding AddRuleCommand}" />
            </Grid>
            <TextBlock Text="{Binding StatusMessage}" Foreground="DarkSlateGray" Margin="0,6,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="4">
            <TextBlock Text="My Notification History" FontWeight="Bold" FontSize="14" Margin="0,0,0,6" />
            <DataGrid ItemsSource="{Binding History}" AutoGenerateColumns="False" IsReadOnly="True" MinHeight="90">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="When" Binding="{Binding When, StringFormat=HH:mm:ss}" Width="*" />
                    <DataGridTextColumn Header="News Type" Binding="{Binding NewsType}" Width="*" />
                    <DataGridTextColumn Header="Channel" Binding="{Binding Channel}" Width="*" />
                    <DataGridTemplateColumn Header="Status" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <TextBlock.Text>
                                        <MultiBinding Converter="{StaticResource DeliveryStatusConverter}">
                                            <Binding Path="Success" />
                                            <Binding Path="ErrorMessage" />
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
