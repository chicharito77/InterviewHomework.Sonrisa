<UserControl x:Class="DailyBugle.Wpf.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:DailyBugle.Wpf.ViewModels"
             xmlns:conv="clr-namespace:DailyBugle.Wpf.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:AdminViewModel}"
             d:DesignWidth="760">
    <UserControl.Resources>
        <conv:DeliveryStatusConverter x:Key="DeliveryStatusConverter" />
    </UserControl.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Registered Users" FontWeight="Bold" FontSize="14" Margin="0,0,0,6" />
        <DataGrid Grid.Row="1" ItemsSource="{Binding Users}" AutoGenerateColumns="False" IsReadOnly="True"
                  MinHeight="90" MaxHeight="160" Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
                <DataGridTextColumn Header="Channel(s)" Binding="{Binding Channels}" Width="*" />
                <DataGridTextColumn Header="Subscribed News Types" Binding="{Binding SubscribedNewsTypes}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="Fire Event" FontWeight="Bold" FontSize="14" Margin="0,0,0,6" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="News Type:" VerticalAlignment="Center" Margin="0,0,6,0" />
                <ComboBox Grid.Column="1" ItemsSource="{Binding NewsTypes}" SelectedItem="{Binding SelectedNewsType}" />
                <TextBlock Grid.Column="2" Text="Title:" VerticalAlignment="Center" Margin="12,0,6,0" />
                <TextBox Grid.Column="3" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
            <Grid Margin="0,6,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Description:" VerticalAlignment="Center" Margin="0,0,6,0" />
                <TextBox Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="DarkSlateGray" />
                <Button Content="Fire Event" Command="{Binding FireEventCommand}" Padding="12,4" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="3">
            <TextBlock Text="Last Dispatch Results" FontWeight="Bold" FontSize="14" Margin="0,0,0,6" />
            <DataGrid ItemsSource="{Binding LastDispatchResults}" AutoGenerateColumns="False" IsReadOnly="True" MinHeight="90">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="User" Binding="{Binding UserName}" Width="2*" />
                    <DataGridTextColumn Header="Channel" Binding="{Binding Channel}" Width="*" />
                    <DataGridTemplateColumn Header="Status" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <TextBlock.Text>
                                        <MultiBinding Converter="{StaticResource DeliveryStatusConverter}">
                                            <Binding Path="Success" />
                                            <Binding Path="ErrorMessage" />
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
