using System.Windows.Controls;

namespace DailyBugle.Wpf.Views;

/// <summary>Interaction logic for UserView.xaml — pure XAML/DataContext binding, no code-behind logic (MVVM).</summary>
public partial class UserView : UserControl
{
    /// <summary>Creates a new <see cref="UserView"/>.</summary>
    public UserView()
    {
        InitializeComponent();
    }
}
